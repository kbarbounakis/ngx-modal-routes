import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-layout',
  template: `
  <router-outlet></router-outlet>
  `,
  styles: [`
  `]
})
export class AppLayoutComponent  {
  
}
